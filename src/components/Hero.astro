---
---
<section id="home" class="hero">
    <div class="hero-bg">
        <img src="/chennai_hero.png" alt="Happy People at Burgerium Chennai" loading="eager">
        <div class="overlay"></div>
    </div>
    <div class="container hero-content">
        <div class="hero-text fade-in-up">
            <span class="subtitle">The Legend Arrives</span>
            <h1>From Kerala to <br> <span class="text-gradient">Chennai</span></h1>
            <p>A legacy born in Calicut (1998), celebrated in Coimbatore & Palakkad. Burger Tree Brand introduces <b>Burgerium</b>—the next evolution of premium dining.</p>
            <div class="hero-btns">
                <a href="#menu" class="btn-primary">Explore Menu</a>
                <a href="#about" class="btn-secondary">Our Story</a>
            </div>
            <div class="hero-meta">
                <div class="pill">25+ Years • Since 1998</div>
                <div class="pill accent">Opening Soon • Anna Nagar</div>
                <div class="pill">Coimbatore • Palakkad • Calicut</div>
            </div>
        </div>
        <div class="hero-visual fade-in-up delay-2">
            <div class="floating-card glass">
                <img src="/peri_burger.png" alt="Gourmet Peri Peri Burger" loading="lazy">
                <div class="card-badge">
                    <span class="star"><i class="fas fa-star"></i></span>
                    <span>Opening Soon in Chennai</span>
                </div>
            </div>
        </div>
    </div>
</section>

<style>
    .hero {
        position: relative;
        min-height: 100vh;
        display: flex;
        align-items: center;
        padding-top: 80px;
        overflow: hidden;
    }

    .hero-bg {
        position: absolute;
        inset: 0;
        z-index: -1;
    }

    .hero-bg img {
        width: 100%;
        height: 100%;
        object-fit: cover;
    }

    .overlay {
        position: absolute;
        inset: 0;
        background: linear-gradient(to right, rgba(12, 12, 14, 0.92) 0%, rgba(12, 12, 14, 0.82) 42%, rgba(12, 12, 14, 0.55) 100%);
    }

    .hero-content {
        display: grid;
        grid-template-columns: 1.2fr 1fr;
        gap: 4rem;
        align-items: center;
        width: 100%;
    }

    .subtitle {
        display: inline-block;
        color: var(--accent-gold);
        text-transform: uppercase;
        letter-spacing: 2px;
        font-size: 0.85rem;
        margin-bottom: 1rem;
        font-weight: 600;
    }

    .hero-text h1 {
        font-size: 4.5rem;
        line-height: 1.1;
        margin-bottom: 1.5rem;
    }

    .hero-text p {
        color: var(--text-secondary);
        font-size: 1.1rem;
        max-width: 500px;
        margin-bottom: 2.5rem;
    }

    .hero-btns {
        display: flex;
        align-items: center;
    }

    .hero-meta {
        display: flex;
        flex-wrap: wrap;
        gap: 0.75rem;
        margin-top: 1.5rem;
    }

    .pill {
        padding: 0.55rem 1.1rem;
        border-radius: 999px;
        font-size: 0.85rem;
        letter-spacing: 0.3px;
        border: 1px solid var(--glass-border);
        background: rgba(255,255,255,0.04);
        color: var(--text-secondary);
    }

    .pill.accent {
        border-color: rgba(250, 162, 39, 0.35);
        background: rgba(250, 162, 39, 0.12);
        color: #f7d7a0;
        font-weight: 600;
    }

    .floating-card {
        position: relative;
        padding: 1.5rem;
        border-radius: 20px;
        transform: perspective(1000px) rotateY(-5deg) rotateX(5deg);
        transition: transform 0.5s ease;
    }

    .floating-card:hover {
        transform: perspective(1000px) rotateY(0) rotateX(0);
    }

    .floating-card img {
        border-radius: 15px;
        box-shadow: 0 20px 40px rgba(0,0,0,0.4);
    }

    .card-badge {
        position: absolute;
        bottom: 30px;
        left: -20px;
        background: var(--accent-gold);
        color: #000;
        padding: 0.8rem 1.5rem;
        border-radius: 50px;
        display: flex;
        align-items: center;
        gap: 0.5rem;
        font-weight: 600;
        box-shadow: 0 10px 20px rgba(0,0,0,0.2);
        white-space: nowrap;
    }

    @media (max-width: 900px) {
        .hero-text h1 { font-size: 3rem; }
        .hero-content {
            grid-template-columns: 1fr;
            text-align: center;
            padding-top: 2rem;
        }
        .hero-text p { margin: 0 auto 2rem; }
        .hero-btns { justify-content: center; }
        .hero-meta { justify-content: center; }
        .pill { font-size: 0.8rem; }
    }
</style>
