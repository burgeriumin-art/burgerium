---
const base = import.meta.env.BASE_URL.replace(/\/$/, '');
---

<section class="craft-section">
    <div class="container">
        <span class="sub-label">The Difference</span>
        <h2 class="section-title">Why We Are <span class="text-orange">Different</span></h2>
        
        <div class="craft-grid">
            <!-- Card 1 -->
            <div class="craft-card scroll-reveal">
                <div class="card-visual">
                    <img src={`${base}/craft_buns.png`} alt="Baking Buns">
                    <div class="overlay-gradient"></div>
                </div>
                <div class="card-info">
                    <h3>House-Made Buns</h3>
                    <p>We bake our own buns fresh every morning. No preservatives, just soft, cloud-like perfection.</p>
                </div>
            </div>

            <!-- Card 2 -->
            <div class="craft-card scroll-reveal delay-100">
                <div class="card-visual">
                    <img src={`${base}/fresh_prep.png`} alt="Fresh Ingredients">
                    <div class="overlay-gradient"></div>
                </div>
                <div class="card-info">
                    <h3>Freshly Prepared</h3>
                    <p>From farm to tray. We chop, slice, and prep our veggies and sauces daily. Validated freshness you can taste.</p>
                </div>
            </div>

            <!-- Card 3 -->
            <div class="craft-card scroll-reveal delay-200">
                <div class="card-visual">
                    <img src={`${base}/craft_grill.png`} alt="Flame Grilling">
                    <div class="overlay-gradient"></div>
                </div>
                <div class="card-info">
                    <h3>Flame Grilled</h3>
                    <p>Classic charcoal searing for that authentic smoky BBQ flavor. 100% real meat, zero fillers.</p>
                </div>
            </div>
        </div>
    </div>
</section>

<style>
    /* ... existing styles ... */
    .craft-grid {
        display: grid;
        grid-template-columns: repeat(3, 1fr); /* 3 Columns now */
        gap: 2rem;
    }

    .craft-card {
        background: #161616;
        border-radius: 16px;
        overflow: hidden;
        border: 1px solid rgba(255,255,255,0.05);
        transition: 0.3s;
    }

    .craft-card:hover {
        transform: translateY(-10px);
        border-color: var(--brand-orange);
    }

    .card-visual {
        height: 300px;
        position: relative;
        overflow: hidden;
    }

    .card-visual img {
        width: 100%;
        height: 100%;
        object-fit: cover;
        transition: 0.5s;
    }

    .craft-card:hover .card-visual img {
        transform: scale(1.1);
    }

    .overlay-gradient {
        position: absolute;
        bottom: 0; left: 0; width: 100%; height: 50%;
        background: linear-gradient(to top, #161616, transparent);
    }

    .card-info {
        padding: 2rem;
        position: relative;
    }

    .card-info h3 {
        font-size: 1.8rem;
        margin-bottom: 1rem;
        color: #fff;
    }

    .card-info p {
        color: #aaa;
        line-height: 1.6;
    }

    @media (max-width: 1024px) {
        .craft-grid { grid-template-columns: 1fr; gap: 2rem; }
        .card-visual { height: 250px; }
    }
</style>
